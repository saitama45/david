$items = App\Models\SupplierItems::where('ItemCode', '112D9A')->get();
foreach($items as $item) {
    echo "ItemCode: {$item->ItemCode} | SupplierCode: {$item->SupplierCode} | Category: {$item->category}\n";
}
exit;
